object Main {
    def main(args: Array[String]): Unit = {
        val argString = args.mkString
        stringToFiniteAutomaton(argString)
    }

    def stringToFiniteAutomaton(str: String): FiniteAutomaton = {
        //parse string and return resulting finite automaton
        null
    }
}
